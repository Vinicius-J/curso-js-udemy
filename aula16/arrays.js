/**
 * Arrays são conjuntos de listas onde se pode colocar vários conteúdos
 * Os arrays também tem são indexados por indíces, mas cada item é um indíce
 * 
 * 
 */

//               0       1        2
const alunos = ['Luiz', 'Maria', 'Nicole'];
// console.log(alunos);
// console.log(alunos[0]); // É possível escolher qual indíce exibir
// console.log(alunos[1]);
// console.log(alunos[2]);

// alunos[0] = 'Eduardo'; // É possível editar o elemento de um indíce
// console.log(alunos);
// console.log(alunos[0]);

// alunos[alunos.length] = 'Luiza'; // Uma forma de adicionar elementos no fim da array
// alunos[alunos.length] = 'Fábio';
// alunos[alunos.length] = 'Luana';

// alunos.push('Luiza'); // Outra forma de adicionar elementos no fim da array
// alunos.push('Fábio');
// alunos.push('Luana');

// alunos.unshift('Luiza'); // Adicionar um elemento no início da array
// alunos.unshift('Fábio');
// alunos.unshift('Luana');

// const removido = alunos.pop(); // Remove o último elemento da array
// console.log(removido);
// const removido = alunos.shift(); // Remove o primeiro elemento da array
// console.log(removido);
console.log(alunos);